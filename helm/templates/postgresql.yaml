{{- include "postgresql.fullname" . }}:
{{- include "postgresql.tplvalues" . | nindent 2 }}

# External Access
{{- if .Values.postgresql.externalAccess.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql.fullname" . }}-external
spec:
  type: {{ .Values.postgresql.externalAccess.service.type }}
  ports:
    - port: {{ .Values.postgresql.externalAccess.service.port }}
  selector:
    app.kubernetes.io/name: {{ include "postgresql.name" . }}
{{- end }}

# Backup Job
{{- if .Values.postgresql.backup.enabled }}
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ include "postgresql.fullname" . }}-backup
spec:
  schedule: "{{ .Values.postgresql.backup.schedule }}"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup
            image: bitnami/postgresql:latest
            command: ["sh", "-c", "pg_dump -U {{ .Values.postgresql.global.postgresql.auth.username }} -h {{ include "postgresql.fullname" . }} {{ .Values.postgresql.global.postgresql.auth.database }} > /backups/backup.sql"]
            env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql-secret
                  key: postgresql-password
            volumeMounts:
            - name: backup-volume
              mountPath: /backups
          restartPolicy: OnFailure
          volumes:
          - name: backup-volume
            persistentVolumeClaim:
              claimName: {{ include "postgresql.fullname" . }}-backup
{{- end }}